# Testnet private key for the headless write+read proof (scripts/write-and-read.ts).
# Use a TESTNET key only. Never commit the real .env. Funds are refundable testnet CKB.
CKB_TESTNET_PRIVATE_KEY=0x...

# Optional: encrypt the document before it touches the chain. Leave unset for a plaintext write.
# CKB_DOC_PASSPHRASE=correct horse battery staple
